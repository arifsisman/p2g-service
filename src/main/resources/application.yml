# configures the port that Eureka Server will be running
server:
  port: 8080

# Data source configuration
spring:
  datasource:
    url: jdbc:postgresql://${db_ip}:5432/p2g_db
    username: postgres
    password: pos
    initialization-mode: never

  jpa:
    hibernate.ddl-auto: create-drop
    properties:
      hibernate:
        show_sql: false
        format_sql: true
        jdbc.lob.non_contextual_creation: true


# Spotify configuration
spotify:
  clientId: 76f07bcdc1054c32884054e43a135480
  clientSecret: 9881b96bb7e24401a9162312598185ba
  redirectUrl: http://localhost:8080/callback
